package com.shriram;

// This class holds the actual temperature value.
// The variable name "temp" must exactly match the key in the JSON.
public class MainData {
    private double temp;

    public double getTemp() { return temp; }
    public void setTemp(double temp) { this.temp = temp; }
}